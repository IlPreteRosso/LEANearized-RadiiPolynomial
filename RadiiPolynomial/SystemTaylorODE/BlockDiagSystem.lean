import RadiiPolynomial.SystemTaylorODE.BlockDiagSystem.Scalar

/-!
# Block-Structured System Operators on `(Seq ν)^L`

Aggregate import for the split BlockDiagSystem modules.

Module tree:
- `BlockDiagSystem/Base`: abstract structural layer
- `BlockDiagSystem/Concrete`: concrete `(ℓ¹_ν)^L` realization
- `BlockDiagSystem/Scalar`: `L = 1` convenience layer
-/
